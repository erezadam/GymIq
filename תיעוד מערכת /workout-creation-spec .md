# הקמת אימון - מסמך אפיון
**גרסה:** 1.0  
**תאריך עדכון:** 03/01/2026

---

## 1. סקירה כללית

מסמך זה מתאר את הזרימה המלאה של יצירת אימון חדש באפליקציית GymIQ.

---

## 2. זרימת המשתמש (User Flow)

```
דשבורד
    │
    ▼
[כפתור: בניית אימון חופשי]
    │
    ▼
מסך בחירת תרגילים
    │
    ├── שדה תאריך (אופציונלי) ← חדש!
    │
    ├── בחירת תרגילים
    │
    ▼
[כפתור: התחל להתאמן]
    │
    ├─── תאריך עתידי? ──► כן ──► שמירה כ"מתוכנן" ──► מסך תוכנית אימונים
    │                                                    (לשונית היסטוריה)
    │
    └─── לא (היום/ריק) ──► שמירה כ"בתהליך" ──► מסך אימון פעיל
```

---

## 3. מסך בחירת תרגילים - שינויים נדרשים

### 3.1 מבנה המסך (מלמעלה למטה)

```
┌─────────────────────────────────────┐
│ ← חזרה          בחר תרגילים         │  Header
├─────────────────────────────────────┤
│                                     │
│  📅 בחר תאריך לאימון (אופציונלי)    │  ← שדה חדש!
│  ┌─────────────────────────────┐    │
│  │  [לחץ לבחירת תאריך]         │    │
│  └─────────────────────────────┘    │
│                                     │
├─────────────────────────────────────┤
│ 🔍 חיפוש תרגיל...                   │  Search
├─────────────────────────────────────┤
│ [חזה] [גב] [כתפיים] [רגליים]...    │  Filters
├─────────────────────────────────────┤
│                                     │
│  ┌─────────────────────────────┐    │
│  │ 🏋️ לחיצת חזה               │    │  Exercise List
│  │ חזה · משקל חופשי           │    │
│  └─────────────────────────────┘    │
│                                     │
│  ┌─────────────────────────────┐    │
│  │ 🏋️ סקוואט                  │    │
│  │ רגליים · מכונה              │    │
│  └─────────────────────────────┘    │
│                                     │
├─────────────────────────────────────┤
│                                     │
│     [  התחל להתאמן (3 תרגילים)  ]   │  Action Button
│                                     │
└─────────────────────────────────────┘
```

### 3.2 שדה התאריך - פרטים טכניים

| פרמטר | ערך |
|-------|-----|
| מיקום | מתחת לכותרת "בחר תרגילים", מעל החיפוש |
| סוג | Date Picker |
| ברירת מחדל | ריק (לא נבחר) |
| טווח תאריכים | היום ועד שנה קדימה |
| פורמט תצוגה | DD/MM/YYYY (עברית) |
| חובה? | לא - אופציונלי |

### 3.3 התנהגות שדה התאריך

```typescript
// מצבים אפשריים
type DateState = 
  | { type: 'empty' }           // לא נבחר תאריך
  | { type: 'today' }           // נבחר תאריך היום
  | { type: 'future', date: Date }  // נבחר תאריך עתידי
```

---

## 4. לוגיקת קביעת סטטוס האימון

### 4.1 קוד הלוגיקה

```typescript
const determineWorkoutFlow = (selectedDate: Date | null): WorkoutFlow => {
  const today = new Date();
  today.setHours(0, 0, 0, 0);
  
  // מקרה 1: לא נבחר תאריך
  if (!selectedDate) {
    return {
      status: 'in_progress',
      scheduledDate: today,
      navigateTo: 'active-workout'
    };
  }
  
  // מקרה 2: נבחר תאריך היום
  const selectedNormalized = new Date(selectedDate);
  selectedNormalized.setHours(0, 0, 0, 0);
  
  if (selectedNormalized.getTime() === today.getTime()) {
    return {
      status: 'in_progress',
      scheduledDate: today,
      navigateTo: 'active-workout'
    };
  }
  
  // מקרה 3: נבחר תאריך עתידי
  return {
    status: 'planned',
    scheduledDate: selectedDate,
    navigateTo: 'workout-planning'  // מסך תוכנית אימונים
  };
};
```

### 4.2 טבלת החלטות

| תאריך שנבחר | סטטוס | צבע | ניווט אחרי שמירה |
|-------------|-------|-----|------------------|
| לא נבחר | `in_progress` | צהוב | מסך אימון פעיל |
| היום | `in_progress` | צהוב | מסך אימון פעיל |
| עתידי | `planned` | אדום | מסך תוכנית אימונים |

---

## 5. שמירה ב-Firebase

### 5.1 מבנה המסמך

```typescript
interface WorkoutDocument {
  id: string;
  
  // זיהוי
  oderId: string;
  
  // תאריכים
  scheduledDate: Timestamp;     // התאריך שנבחר (או היום)
  createdAt: Timestamp;         // מתי נוצר
  startedAt?: Timestamp;        // מתי התחילו לבצע (רק אם in_progress)
  completedAt?: Timestamp;      // מתי הושלם
  
  // סטטוס
  status: 'planned' | 'in_progress' | 'completed';
  
  // תרגילים
  exercises: WorkoutExercise[];
  exerciseCount: number;
  
  // מטא-דאטה
  muscleGroups: string[];       // לתצוגה מהירה
}
```

### 5.2 פונקציית השמירה

```typescript
const createWorkout = async (
  exercises: Exercise[],
  selectedDate: Date | null
): Promise<void> => {
  
  const flow = determineWorkoutFlow(selectedDate);
  
  const workout: WorkoutDocument = {
    id: generateId(),
    userId: currentUser.uid,
    exercises: exercises.map(ex => ({
      exerciseId: ex.id,
      name: ex.name,
      sets: [],
      isCompleted: false
    })),
    exerciseCount: exercises.length,
    muscleGroups: [...new Set(exercises.map(ex => ex.muscleGroup))],
    scheduledDate: Timestamp.fromDate(flow.scheduledDate),
    createdAt: serverTimestamp(),
    status: flow.status,
    ...(flow.status === 'in_progress' && { startedAt: serverTimestamp() })
  };
  
  // שמירה ב-Firebase
  await addDoc(collection(db, 'workouts'), workout);
  
  // ניווט
  if (flow.navigateTo === 'active-workout') {
    navigate(`/workout/${workout.id}`);
  } else {
    showToast(`האימון נשמר לתאריך ${formatDate(flow.scheduledDate)}`);
    navigate('/workout-planning');
  }
};
```

---

## 6. מסך תוכנית אימונים - תצוגת אימונים מתוכננים

### 6.1 סדר התצוגה

אימונים מתוכננים (status: 'planned') מוצגים:
1. **בראש הרשימה** - לפני כל האימונים האחרים
2. **בסדר עולה** - הקרוב ביותר קודם

```typescript
const sortWorkouts = (workouts: Workout[]): Workout[] => {
  // 1. אימונים מתוכננים - סדר עולה (מחר לפני מחרתיים)
  const planned = workouts
    .filter(w => w.status === 'planned')
    .sort((a, b) => a.scheduledDate.toMillis() - b.scheduledDate.toMillis());
  
  // 2. שאר האימונים - סדר יורד (החדש קודם)
  const others = workouts
    .filter(w => w.status !== 'planned')
    .sort((a, b) => b.scheduledDate.toMillis() - a.scheduledDate.toMillis());
  
  return [...planned, ...others];
};
```

### 6.2 עיצוב כרטיס אימון מתוכנן

| מאפיין | ערך |
|--------|-----|
| צבע רקע/גבול | אדום (#EF4444 או tokens.error) |
| אייקון | 📅 או שעון |
| טקסט סטטוס | "מתוכנן ל-DD/MM" |
| כפתור פעולה | "התחל אימון" |

---

## 7. קבצים לעדכון

```
src/
├── pages/
│   └── ExerciseSelectionPage.tsx    // הוספת שדה תאריך
│
├── components/
│   └── exercise-selection/
│       ├── DatePickerField.tsx      // קומפוננטה חדשה
│       └── StartWorkoutButton.tsx   // עדכון לוגיקה
│
├── hooks/
│   └── useCreateWorkout.ts          // לוגיקת יצירה
│
└── lib/
    └── firebase/
        └── workoutService.ts        // פונקציות Firebase
```

---

## 8. רשימת משימות (Checklist)

- [ ] הוספת קומפוננטת DatePickerField למסך בחירת תרגילים
- [ ] עדכון לוגיקת כפתור "התחל להתאמן"
- [ ] יצירת hook: useCreateWorkout
- [ ] עדכון workoutService עם פונקציית createWorkout
- [ ] עדכון מסך תוכנית אימונים לתמוך בסידור הנכון
- [ ] בדיקה: יצירת אימון ללא תאריך
- [ ] בדיקה: יצירת אימון לתאריך היום
- [ ] בדיקה: יצירת אימון לתאריך עתידי

---

## 9. הנחיות לסוכן - בדיקה עצמית (Agent Self-Verification)

### 9.1 הנחיות כלליות

> **חשוב:** לפני סיום המשימה, הסוכן חייב לבצע את כל הבדיקות המפורטות למטה ולוודא שהמערכת עובדת בדיוק לפי האפיון. אין לדווח על סיום המשימה עד שכל הבדיקות עוברות בהצלחה.

### 9.2 בדיקות UI - מסך בחירת תרגילים

```
□ בדיקה 1: שדה תאריך קיים
  - פתח את מסך בחירת תרגילים
  - ודא: שדה בחירת תאריך מופיע מתחת לכותרת "בחר תרגילים"
  - ודא: השדה מופיע מעל שדה החיפוש
  - ודא: השדה מסומן כאופציונלי (לא חובה)
  
□ בדיקה 2: פונקציונליות שדה תאריך
  - לחץ על שדה התאריך
  - ודא: Date Picker נפתח
  - ודא: לא ניתן לבחור תאריכים בעבר (רק היום והלאה)
  - ודא: פורמט התאריך בעברית DD/MM/YYYY
  
□ בדיקה 3: כפתור התחל להתאמן
  - ודא: הכפתור מופיע בתחתית המסך
  - ודא: הכפתור מציג מספר תרגילים שנבחרו
  - ודא: הכפתור מושבת כשאין תרגילים נבחרים
```

### 9.3 בדיקות לוגיקה - תרחישי יצירת אימון

```
□ תרחיש A: יצירת אימון ללא בחירת תאריך
  1. פתח מסך בחירת תרגילים
  2. בחר 2-3 תרגילים
  3. אל תבחר תאריך (השאר ריק)
  4. לחץ "התחל להתאמן"
  
  ודא:
  ✓ המערכת יוצרת אימון עם status: 'in_progress'
  ✓ scheduledDate = תאריך היום
  ✓ הניווט מעביר למסך אימון פעיל
  ✓ האימון מופיע ב-Firebase עם הנתונים הנכונים

□ תרחיש B: יצירת אימון לתאריך היום
  1. פתח מסך בחירת תרגילים
  2. בחר 2-3 תרגילים
  3. בחר את תאריך היום ב-Date Picker
  4. לחץ "התחל להתאמן"
  
  ודא:
  ✓ המערכת יוצרת אימון עם status: 'in_progress'
  ✓ הניווט מעביר למסך אימון פעיל
  ✓ ההתנהגות זהה לתרחיש A

□ תרחיש C: יצירת אימון לתאריך עתידי (מחר)
  1. פתח מסך בחירת תרגילים
  2. בחר 2-3 תרגילים
  3. בחר תאריך מחר ב-Date Picker
  4. לחץ "התחל להתאמן"
  
  ודא:
  ✓ המערכת יוצרת אימון עם status: 'planned'
  ✓ scheduledDate = התאריך שנבחר
  ✓ מופיעה הודעת toast: "האימון נשמר לתאריך XX/XX"
  ✓ הניווט מעביר למסך תוכנית אימונים
  ✓ האימון לא מופיע במסך אימון פעיל

□ תרחיש D: יצירת אימון לתאריך עתידי (עוד שבוע)
  1. חזור על תרחיש C עם תאריך עוד 7 ימים
  
  ודא:
  ✓ כל הבדיקות של תרחיש C עוברות
```

### 9.4 בדיקות מסך תוכנית אימונים

```
□ בדיקה 1: תצוגת אימון מתוכנן
  - צור אימון מתוכנן למחר (תרחיש C)
  - עבור למסך תוכנית אימונים
  
  ודא:
  ✓ האימון מופיע בראש הרשימה
  ✓ האימון מסומן בצבע אדום
  ✓ מוצג תאריך האימון המתוכנן
  ✓ יש כפתור "התחל אימון"

□ בדיקה 2: סדר אימונים מתוכננים
  - צור אימון מתוכנן למחר
  - צור אימון מתוכנן לעוד 3 ימים
  - צור אימון מתוכנן למחרתיים
  - עבור למסך תוכנית אימונים
  
  ודא:
  ✓ הסדר מלמעלה למטה: מחר → מחרתיים → עוד 3 ימים
  ✓ כל האימונים המתוכננים מופיעים לפני אימונים אחרים

□ בדיקה 3: שילוב עם אימונים אחרים
  - ודא שיש אימונים קיימים (completed / in_progress)
  - צור אימון מתוכנן
  - עבור למסך תוכנית אימונים
  
  ודא:
  ✓ אימונים מתוכננים (אדום) בראש הרשימה
  ✓ אחריהם אימונים בתהליך (צהוב)
  ✓ אחריהם אימונים שהושלמו (כחול)
```

### 9.5 בדיקות Firebase

```
□ בדיקה 1: מבנה המסמך
  - צור אימון חדש
  - פתח Firebase Console > Firestore
  - מצא את המסמך שנוצר
  
  ודא שהמסמך מכיל:
  ✓ id: string (לא ריק)
  ✓ oderId: string (מזהה המשתמש הנוכחי)
  ✓ status: 'planned' | 'in_progress'
  ✓ scheduledDate: Timestamp
  ✓ createdAt: Timestamp
  ✓ exercises: array (עם התרגילים שנבחרו)
  ✓ exerciseCount: number (מספר נכון)
  ✓ muscleGroups: array

□ בדיקה 2: נכונות הנתונים
  - צור אימון עם 3 תרגילים לתאריך עתידי
  - בדוק ב-Firebase:
  
  ודא:
  ✓ exerciseCount === 3
  ✓ exercises.length === 3
  ✓ status === 'planned'
  ✓ scheduledDate === התאריך שנבחר
  ✓ startedAt לא קיים (כי האימון מתוכנן)
```

### 9.6 בדיקות שליליות (Edge Cases)

```
□ בדיקה 1: ניסיון ללא תרגילים
  - פתח מסך בחירת תרגילים
  - אל תבחר תרגילים
  - נסה ללחוץ "התחל להתאמן"
  
  ודא:
  ✓ הכפתור מושבת או מופיעה הודעת שגיאה
  ✓ לא נוצר אימון ב-Firebase

□ בדיקה 2: בחירת תאריך עבר
  - ודא שה-Date Picker לא מאפשר בחירת תאריכים בעבר
  - אם אפשר לבחור → באג! יש לתקן

□ בדיקה 3: ניווט אחורה
  - התחל ליצור אימון
  - בחר תרגילים
  - לחץ חזרה (back)
  - חזור למסך
  
  ודא:
  ✓ הבחירות לא נשמרו (התחלה מחדש)
  ✓ או: הבחירות נשמרו (לפי התנהגות רצויה)
```

### 9.7 דוח בדיקה לסוכן

לאחר ביצוע כל הבדיקות, הסוכן ידווח בפורמט הבא:

```markdown
## דוח בדיקה עצמית - הקמת אימון

**תאריך:** [תאריך]
**סוכן:** [שם/גרסה]

### תוצאות בדיקות UI
| בדיקה | תוצאה | הערות |
|-------|-------|-------|
| שדה תאריך קיים | ✅/❌ | |
| פונקציונליות Date Picker | ✅/❌ | |
| כפתור התחל להתאמן | ✅/❌ | |

### תוצאות תרחישי יצירה
| תרחיש | תוצאה | הערות |
|--------|-------|-------|
| A - ללא תאריך | ✅/❌ | |
| B - תאריך היום | ✅/❌ | |
| C - תאריך מחר | ✅/❌ | |
| D - תאריך עתידי | ✅/❌ | |

### תוצאות מסך תוכנית אימונים
| בדיקה | תוצאה | הערות |
|-------|-------|-------|
| תצוגה בצבע אדום | ✅/❌ | |
| סדר עולה | ✅/❌ | |
| מיקום בראש | ✅/❌ | |

### תוצאות Firebase
| בדיקה | תוצאה | הערות |
|-------|-------|-------|
| מבנה מסמך | ✅/❌ | |
| נכונות נתונים | ✅/❌ | |

### בעיות שנמצאו
1. [תיאור בעיה]
2. [תיאור בעיה]

### סטטוס סופי
[ ] כל הבדיקות עברו - המשימה הושלמה
[ ] יש בעיות - נדרש תיקון
```

---

---

## 11. המשך לאימון - ממסך היסטוריה

### 11.1 זרימת המשתמש

```
מסך תוכנית אימונים (היסטוריה)
    │
    ▼
לחיצה על כרטיס אימון
    │
    ▼
הכרטיס מתרחב - מציג רשימת תרגילים
    │
    ▼
לחיצה על כפתור "המשך לאימון"
    │
    ▼
פופאפ אישור
    │
    ├── ביטול ──► חזרה למסך
    │
    └── "התחל את האימון" ──► לוגיקה לפי סטטוס ──► מסך אימון פעיל
```

### 11.2 לוגיקת העתקה לפי סטטוס

```typescript
const handleStartWorkout = (workout: WorkoutDocument) => {
  
  // ════════════════════════════════════════════════════════════════
  // מקרה 1: אימון שהושלם (completed) - כחול
  // ════════════════════════════════════════════════════════════════
  if (workout.status === 'completed') {
    // מעתיקים רק את התרגילים, בלי נתוני ביצוע
    const newWorkout: WorkoutDocument = {
      id: generateId(),
      userId: currentUser.uid,
      
      // תרגילים בלי נתונים
      exercises: workout.exercises.map(ex => ({
        exerciseId: ex.exerciseId,
        name: ex.name,
        sets: [],              // ← ריק! בלי סטים
        isCompleted: false     // ← לא הושלם
      })),
      
      exerciseCount: workout.exerciseCount,
      completedExerciseCount: 0,  // ← אפס
      muscleGroups: workout.muscleGroups,
      
      // תאריכים חדשים
      scheduledDate: Timestamp.now(),
      createdAt: serverTimestamp(),
      startedAt: serverTimestamp(),
      
      // סטטוס חדש
      status: 'in_progress'
    };
    
    await workoutService.create(newWorkout);
    navigateToActiveWorkout(newWorkout.id);
  }
  
  // ════════════════════════════════════════════════════════════════
  // מקרה 2: אימון בתהליך (in_progress) - צהוב
  // ════════════════════════════════════════════════════════════════
  else if (workout.status === 'in_progress') {
    // מעתיקים הכל כולל נתוני ביצוע חלקיים
    const newWorkout: WorkoutDocument = {
      id: generateId(),
      userId: currentUser.uid,
      
      // תרגילים עם כל הנתונים הקיימים
      exercises: workout.exercises.map(ex => ({
        exerciseId: ex.exerciseId,
        name: ex.name,
        sets: [...ex.sets],        // ← מעתיקים את הסטים הקיימים
        isCompleted: ex.isCompleted // ← שומרים סטטוס
      })),
      
      exerciseCount: workout.exerciseCount,
      completedExerciseCount: workout.completedExerciseCount, // ← שומרים
      muscleGroups: workout.muscleGroups,
      
      // תאריכים
      scheduledDate: Timestamp.now(),
      createdAt: serverTimestamp(),
      startedAt: serverTimestamp(),
      
      // סטטוס
      status: 'in_progress'
    };
    
    await workoutService.create(newWorkout);
    navigateToActiveWorkout(newWorkout.id);
  }
  
  // ════════════════════════════════════════════════════════════════
  // מקרה 3: אימון מתוכנן (planned) - אדום
  // ════════════════════════════════════════════════════════════════
  else if (workout.status === 'planned') {
    // מעדכנים את האימון הקיים ומתחילים אותו
    await workoutService.update(workout.id, {
      status: 'in_progress',
      startedAt: serverTimestamp(),
      scheduledDate: Timestamp.now()  // מעדכנים להיום
    });
    
    navigateToActiveWorkout(workout.id);  // ← אותו ID, לא יוצרים חדש
  }
};
```

### 11.3 טבלת החלטות - המשך לאימון

| סטטוס מקורי | צבע | פעולה | תרגילים | נתוני ביצוע | אימון חדש? |
|-------------|-----|-------|---------|-------------|------------|
| `completed` | כחול | העתקה נקייה | ✅ מועתקים | ❌ לא מועתקים | ✅ כן |
| `in_progress` | צהוב | העתקה מלאה | ✅ מועתקים | ✅ מועתקים | ✅ כן |
| `planned` | אדום | התחלה | ✅ קיימים | - | ❌ לא (עדכון) |

### 11.4 עיצוב הפופאפ

```
┌─────────────────────────────────────────┐
│                                         │
│         התחלת אימון                     │
│                                         │
│  ┌─────────────────────────────────┐    │
│  │                                 │    │
│  │   [הודעה משתנה לפי סטטוס]       │    │
│  │                                 │    │
│  └─────────────────────────────────┘    │
│                                         │
│  ┌─────────────┐    ┌─────────────┐     │
│  │   ביטול     │    │ התחל אימון  │     │
│  └─────────────┘    └─────────────┘     │
│                                         │
└─────────────────────────────────────────┘
```

### 11.5 הודעות הפופאפ לפי סטטוס

```typescript
const getPopupMessage = (status: WorkoutStatus): string => {
  switch (status) {
    case 'completed':
      return 'שים לב: אתה מתחיל אימון חדש מאפס עם אותם תרגילים';
    
    case 'in_progress':
      return 'שים לב: אתה ממשיך את האימון הקיים עם כל הנתונים שדיווחת';
    
    case 'planned':
      return 'שים לב: אתה מתחיל את האימון המתוכנן';
    
    default:
      return '';
  }
};
```

### 11.6 מבנה נתונים - WorkoutExercise

```typescript
interface WorkoutExercise {
  exerciseId: string;
  name: string;
  
  // סטים שבוצעו
  sets: ExerciseSet[];
  
  // האם התרגיל הושלם
  isCompleted: boolean;
}

interface ExerciseSet {
  setNumber: number;
  weight: number;      // משקל בק"ג
  reps: number;        // חזרות
  isCompleted: boolean;
}
```

### 11.7 קבצים לעדכון

```
src/
├── components/
│   └── workout-planning/
│       ├── WorkoutCard.tsx              // הוספת כפתור "המשך לאימון"
│       ├── ContinueWorkoutDialog.tsx    // פופאפ חדש
│       └── WorkoutExerciseList.tsx      // רשימת תרגילים מורחבת
│
├── hooks/
│   └── useContinueWorkout.ts            // לוגיקת המשך אימון
│
└── lib/firebase/
    └── workoutService.ts                // הוספת פונקציות העתקה
```

### 11.8 בדיקות - המשך לאימון

```
□ תרחיש A: המשך מאימון שהושלם (כחול)
  1. צור אימון והשלם אותו (status: completed)
  2. עבור למסך היסטוריה
  3. לחץ על האימון שהושלם
  4. לחץ "המשך לאימון"
  5. אשר בפופאפ
  
  ודא:
  ✓ הודעת הפופאפ: "מתחיל אימון חדש מאפס"
  ✓ נוצר אימון חדש ב-Firebase (ID חדש)
  ✓ התרגילים מועתקים
  ✓ אין נתוני סטים (sets ריק)
  ✓ isCompleted = false לכל תרגיל
  ✓ status = 'in_progress'
  ✓ הניווט למסך אימון פעיל

□ תרחיש B: המשך מאימון בתהליך (צהוב)
  1. צור אימון ודווח על חלק מהתרגילים
  2. צא מהאימון (status: in_progress)
  3. עבור למסך היסטוריה
  4. לחץ על האימון הצהוב
  5. לחץ "המשך לאימון"
  6. אשר בפופאפ
  
  ודא:
  ✓ הודעת הפופאפ: "ממשיך את האימון הקיים"
  ✓ נוצר אימון חדש ב-Firebase
  ✓ התרגילים מועתקים עם כל הנתונים
  ✓ הסטים שדווחו קיימים
  ✓ isCompleted נשמר לכל תרגיל
  ✓ במסך אימון פעיל - רואים את הדיווח הקודם

□ תרחיש C: התחלת אימון מתוכנן (אדום)
  1. צור אימון מתוכנן (תאריך עתידי)
  2. עבור למסך היסטוריה
  3. לחץ על האימון האדום
  4. לחץ "המשך לאימון"
  5. אשר בפופאפ
  
  ודא:
  ✓ הודעת הפופאפ: "מתחיל את האימון המתוכנן"
  ✓ לא נוצר אימון חדש (אותו ID)
  ✓ status השתנה ל-'in_progress'
  ✓ startedAt עודכן
  ✓ הניווט למסך אימון פעיל

□ בדיקה D: ביטול בפופאפ
  1. לחץ על אימון כלשהו
  2. לחץ "המשך לאימון"
  3. לחץ "ביטול" בפופאפ
  
  ודא:
  ✓ הפופאפ נסגר
  ✓ לא נוצר אימון חדש
  ✓ נשאר במסך היסטוריה
```

### 11.9 ניווט למסך אימון פעיל - קריטי!

> ⚠️ **שים לב:** לאחר לחיצה על "התחל אימון" בפופאפ, המערכת חייבת לנווט 
> למסך **אימון פעיל** ולא לשום מסך אחר!

#### נתיבים נכונים (בדוק מה קיים בפרויקט)

```typescript
// אחת מהאפשרויות הבאות - בדוק ב-router שלך:
navigate(`/workout/${workoutId}`);
navigate(`/active-workout/${workoutId}`);
navigate(`/workout-session/${workoutId}`);
```

#### נתיבים שגויים - לא לנווט אליהם!

```typescript
// ❌ שגוי - מסך ספריית תרגילים
navigate('/exercises');
navigate('/exercise-library');

// ❌ שגוי - מסך בחירת תרגילים
navigate('/exercise-selection');

// ❌ שגוי - מסך היסטוריה
navigate('/workout-planning');
navigate('/history');
```

#### איך למצוא את הנתיב הנכון

```
1. פתח את App.tsx או router.tsx
2. חפש את הקומפוננטה: ActiveWorkout / WorkoutSession / WorkoutActive
3. מצא את ה-path שלה
4. השתמש בנתיב הזה בפונקציית הניווט
```

#### קוד לדוגמה - ניווט נכון

```typescript
const handleConfirmContinue = async () => {
  let targetWorkoutId: string;
  
  if (workout.status === 'completed') {
    const newWorkout = await createWorkoutWithoutData(workout);
    targetWorkoutId = newWorkout.id;
    
  } else if (workout.status === 'in_progress') {
    const newWorkout = await createWorkoutWithData(workout);
    targetWorkoutId = newWorkout.id;
    
  } else if (workout.status === 'planned') {
    await updateWorkoutStatus(workout.id, 'in_progress');
    targetWorkoutId = workout.id;
  }
  
  // ════════════════════════════════════════════════════════════
  // 🚨 קריטי: ניווט למסך אימון פעיל בלבד!
  // ════════════════════════════════════════════════════════════
  navigate(`/workout/${targetWorkoutId}`);
  
  // סגירת הפופאפ
  setIsDialogOpen(false);
};
```

---

## 12. מבנה נתונים ב-Firestore

### 12.1 סקירה כללית

> ⚠️ **עיקרון חשוב:** כל הנתונים הדינמיים (שרירים, ציוד, קטגוריות) חייבים להישמר 
> ב-Firestore ולא כ-hardcoded בקוד. זה מאפשר ניהול דרך ממשק ללא שינוי קוד.

### 12.2 Collections נדרשים

```
firestore/
├── users/                    # משתמשים
├── workouts/                 # אימונים
├── exercises/                # תרגילים
├── muscles/                  # 🆕 שרירים (לא hardcoded!)
└── equipment/                # 🆕 ציוד (לא hardcoded!)
```

### 12.3 Collection: muscles (שרירים)

```typescript
// Firestore path: /muscles/{muscleId}

interface MuscleDocument {
  id: string;              // מזהה ייחודי: "chest", "back", "arms"
  name: string;            // שם באנגלית: "Arms"
  hebrewName: string;      // שם בעברית: "זרועות"
  icon?: string;           // אייקון (אופציונלי): "💪"
  
  // תתי שרירים
  subMuscles: SubMuscle[]; // רשימת תתי שרירים
  
  // תצוגה
  order: number;           // סדר תצוגה ברשימה: 1=ראשון, 2=שני, וכו'
  isActive: boolean;       // האם פעיל: true
  
  // מטא-דאטה
  createdAt: Timestamp;
  updatedAt: Timestamp;
}

interface SubMuscle {
  id: string;              // מזהה: "biceps", "triceps"
  name: string;            // שם באנגלית: "Biceps"
  hebrewName: string;      // שם בעברית: "בייספס"
}
```

#### הסבר על שדה order

```
order: number - קובע את סדר התצוגה ברשימה

לדוגמה:
  חזה    order: 1  ← יופיע ראשון
  גב     order: 2  ← יופיע שני
  כתפיים order: 3  ← יופיע שלישי
  ...
  אחר    order: 99 ← יופיע אחרון

זה מאפשר לשנות את הסדר בלי לשנות קוד - רק עדכון ב-Database
```

#### נתוני התחלה - muscles

```typescript
const initialMuscles = [
  { 
    id: "chest",
    name: "Chest",
    hebrewName: "חזה",
    order: 1,
    subMuscles: [
      { id: "upper_chest", name: "Upper Chest", hebrewName: "חזה עליון" },
      { id: "middle_chest", name: "Middle Chest", hebrewName: "חזה אמצעי" },
      { id: "lower_chest", name: "Lower Chest", hebrewName: "חזה תחתון" }
    ]
  },
  { 
    id: "back",
    name: "Back",
    hebrewName: "גב",
    order: 2,
    subMuscles: [
      { id: "lats", name: "Lats", hebrewName: "לאטס (רוחב)" },
      { id: "upper_back", name: "Upper Back", hebrewName: "גב עליון" },
      { id: "lower_back", name: "Lower Back", hebrewName: "גב תחתון" },
      { id: "traps", name: "Traps", hebrewName: "טרפז" }
    ]
  },
  { 
    id: "shoulders",
    name: "Shoulders",
    hebrewName: "כתפיים",
    order: 3,
    subMuscles: [
      { id: "front_delt", name: "Front Delt", hebrewName: "כתף קדמית" },
      { id: "side_delt", name: "Side Delt", hebrewName: "כתף צידית" },
      { id: "rear_delt", name: "Rear Delt", hebrewName: "כתף אחורית" }
    ]
  },
  { 
    id: "arms",
    name: "Arms",
    hebrewName: "זרועות",
    order: 4,
    subMuscles: [
      { id: "biceps", name: "Biceps", hebrewName: "בייספס" },
      { id: "triceps", name: "Triceps", hebrewName: "טרייספס" },
      { id: "forearms", name: "Forearms", hebrewName: "אמות" }
    ]
  },
  { 
    id: "legs",
    name: "Legs",
    hebrewName: "רגליים",
    order: 5,
    subMuscles: [
      { id: "quads", name: "Quadriceps", hebrewName: "ארבע ראשי (קדמי)" },
      { id: "hamstrings", name: "Hamstrings", hebrewName: "אחורי ירך" },
      { id: "glutes", name: "Glutes", hebrewName: "ישבן" },
      { id: "calves", name: "Calves", hebrewName: "תאומים (שוק)" }
    ]
  },
  { 
    id: "core",
    name: "Core",
    hebrewName: "בטן",
    order: 6,
    subMuscles: [
      { id: "abs", name: "Abs", hebrewName: "שריר בטן ישר" },
      { id: "obliques", name: "Obliques", hebrewName: "אלכסוניים" },
      { id: "lower_abs", name: "Lower Abs", hebrewName: "בטן תחתונה" }
    ]
  },
  { 
    id: "cardio",
    name: "Cardio",
    hebrewName: "אירובי",
    order: 7,
    subMuscles: []  // אין תתי שרירים
  },
  { 
    id: "other",
    name: "Other",
    hebrewName: "אחר",
    order: 99,
    subMuscles: []
  }
];
```

### 12.4 Collection: equipment (ציוד)

```typescript
// Firestore path: /equipment/{equipmentId}

interface EquipmentDocument {
  id: string;              // מזהה: "barbell", "dumbbell", "machine"
  name: string;            // שם באנגלית: "Barbell"
  hebrewName: string;      // שם בעברית: "מוט"
  icon?: string;           // אייקון: "🏋️"
  order: number;           // סדר תצוגה
  isActive: boolean;
  createdAt: Timestamp;
  updatedAt: Timestamp;
}
```

#### נתוני התחלה - equipment

```typescript
const initialEquipment = [
  { id: "barbell",    name: "Barbell",    hebrewName: "מוט",          order: 1 },
  { id: "dumbbell",   name: "Dumbbell",   hebrewName: "משקולת יד",    order: 2 },
  { id: "machine",    name: "Machine",    hebrewName: "מכונה",        order: 3 },
  { id: "cable",      name: "Cable",      hebrewName: "כבל",          order: 4 },
  { id: "bodyweight", name: "Bodyweight", hebrewName: "משקל גוף",     order: 5 },
  { id: "kettlebell", name: "Kettlebell", hebrewName: "קטלבל",        order: 6 },
  { id: "bands",      name: "Bands",      hebrewName: "גומיות",       order: 7 },
  { id: "other",      name: "Other",      hebrewName: "אחר",          order: 99 }
];
```

### 12.5 Collection: exercises (תרגילים)

```typescript
// Firestore path: /exercises/{exerciseId}

interface ExerciseDocument {
  id: string;
  name: string;                    // שם באנגלית
  hebrewName: string;              // שם בעברית
  
  // קישור לשרירים (לפי ID מ-collection muscles)
  mainMuscle: string;              // שריר ראשי: "chest"
  secondaryMuscles?: string[];     // שרירים משניים: ["triceps", "shoulders"]
  
  // קישור לציוד (לפי ID מ-collection equipment)
  equipment: string;               // ציוד: "barbell"
  
  // מידע נוסף
  description?: string;
  instructions?: string[];
  imageUrls: string[];
  videoUrl?: string;
  
  // הגדרות מעקב
  trackingType: 'weight' | 'time' | 'distance' | 'reps_only';
  
  // מטא-דאטה
  isActive: boolean;
  order: number;
  createdAt: Timestamp;
  updatedAt: Timestamp;
}
```

### 12.6 Service לטעינת נתונים

```typescript
// lib/firebase/muscleService.ts

export const muscleService = {
  // טעינת כל השרירים
  getAll: async (): Promise<MuscleDocument[]> => {
    const snapshot = await getDocs(
      query(
        collection(db, 'muscles'),
        where('isActive', '==', true),
        orderBy('order', 'asc')
      )
    );
    return snapshot.docs.map(doc => doc.data() as MuscleDocument);
  },
  
  // טעינת שריר בודד
  getById: async (id: string): Promise<MuscleDocument | null> => {
    const doc = await getDoc(doc(db, 'muscles', id));
    return doc.exists() ? doc.data() as MuscleDocument : null;
  },
  
  // הוספת שריר (admin)
  create: async (muscle: Omit<MuscleDocument, 'createdAt' | 'updatedAt'>) => {
    await setDoc(doc(db, 'muscles', muscle.id), {
      ...muscle,
      createdAt: serverTimestamp(),
      updatedAt: serverTimestamp()
    });
  },
  
  // עדכון שריר (admin)
  update: async (id: string, data: Partial<MuscleDocument>) => {
    await updateDoc(doc(db, 'muscles', id), {
      ...data,
      updatedAt: serverTimestamp()
    });
  },
  
  // מחיקה רכה (admin)
  delete: async (id: string) => {
    await updateDoc(doc(db, 'muscles', id), {
      isActive: false,
      updatedAt: serverTimestamp()
    });
  }
};
```

### 12.7 Hook לשימוש בקומפוננטות

```typescript
// hooks/useMuscles.ts

export const useMuscles = () => {
  const [muscles, setMuscles] = useState<MuscleDocument[]>([]);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<Error | null>(null);
  
  useEffect(() => {
    const loadMuscles = async () => {
      try {
        setLoading(true);
        const data = await muscleService.getAll();
        setMuscles(data);
      } catch (err) {
        setError(err as Error);
      } finally {
        setLoading(false);
      }
    };
    
    loadMuscles();
  }, []);
  
  return { muscles, loading, error };
};
```

### 12.8 פונקציית Seed - טעינת נתונים ראשונית

```typescript
// lib/firebase/seedData.ts

export const seedMuscles = async () => {
  const musclesRef = collection(db, 'muscles');
  
  // בדיקה אם כבר יש נתונים
  const snapshot = await getDocs(musclesRef);
  if (!snapshot.empty) {
    console.log('Muscles already exist, skipping seed');
    return;
  }
  
  // טעינת נתונים ראשוניים
  const batch = writeBatch(db);
  
  initialMuscles.forEach(muscle => {
    const docRef = doc(musclesRef, muscle.id);
    batch.set(docRef, {
      ...muscle,
      isActive: true,
      createdAt: serverTimestamp(),
      updatedAt: serverTimestamp()
    });
  });
  
  await batch.commit();
  console.log('Muscles seeded successfully!');
};

export const seedEquipment = async () => {
  // אותו דבר עבור equipment
};

// פונקציה ראשית לטעינת כל הנתונים
export const seedAllData = async () => {
  await seedMuscles();
  await seedEquipment();
  console.log('All data seeded!');
};
```

### 12.9 קבצים ליצירה

```
src/
├── lib/
│   └── firebase/
│       ├── muscleService.ts      // CRUD לשרירים
│       ├── equipmentService.ts   // CRUD לציוד
│       └── seedData.ts           // טעינת נתונים ראשונית
│
├── hooks/
│   ├── useMuscles.ts             // Hook לשרירים
│   └── useEquipment.ts           // Hook לציוד
│
└── types/
    └── database.ts               // TypeScript interfaces
```

### 12.10 בדיקות - מבנה נתונים

```
□ בדיקה 1: Collection muscles קיים
  - פתח Firebase Console
  - ודא: יש Collection בשם "muscles"
  - ודא: יש לפחות 8 מסמכים (שרירים)

□ בדיקה 2: Collection equipment קיים
  - ודא: יש Collection בשם "equipment"
  - ודא: יש לפחות 8 מסמכים (סוגי ציוד)

□ בדיקה 3: טעינה באפליקציה
  - פתח את מסך ניהול -> לשונית שרירים
  - ודא: כל השרירים מופיעים
  - ודא: השמות בעברית נכונים

□ בדיקה 4: סינון תרגילים
  - עבור לספריית תרגילים
  - בחר שריר מהרשימה
  - ודא: התרגילים מסוננים נכון
```

---

### 12.11 ממשק ניהול שרירים וציוד (Admin UI)

> **עיקרון:** מסך הניהול מאפשר לאדמין להוסיף, לערוך ולמחוק שרירים וציוד 
> ללא צורך בשינוי קוד. כל השינויים נשמרים ב-Firebase.

#### מבנה מסך הניהול

```
┌─────────────────────────────────────────────────────────────┐
│  ניהול מערכת                                                │
├─────────────────────────────────────────────────────────────┤
│  [תרגילים]  [שרירים]  [ציוד]  [משתמשים]                    │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   (תוכן משתנה לפי לשונית נבחרת)                            │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### לשונית שרירים - UI

```
┌─────────────────────────────────────────────────────────────┐
│  שרירים                                    [+ הוסף שריר]   │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ 💪 חזה (Chest)                           סדר: 1       │ │
│  │ תתי שרירים: חזה עליון, חזה אמצעי, חזה תחתון           │ │
│  │                                    [✏️ ערוך] [🗑️ מחק] │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                             │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ 💪 גב (Back)                             סדר: 2       │ │
│  │ תתי שרירים: לאטס, גב עליון, גב תחתון, טרפז            │ │
│  │                                    [✏️ ערוך] [🗑️ מחק] │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                             │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ 💪 זרועות (Arms)                         סדר: 4       │ │
│  │ תתי שרירים: בייספס, טרייספס, אמות                     │ │
│  │                                    [✏️ ערוך] [🗑️ מחק] │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                             │
│  ...                                                        │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### פופאפ הוספה/עריכת שריר

```
┌─────────────────────────────────────────────────────────────┐
│  הוספת שריר חדש                                    [✖️]    │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  שם בעברית:     [____________________]                     │
│                                                             │
│  שם באנגלית:    [____________________]                     │
│                                                             │
│  סדר תצוגה:     [___] (מספר)                               │
│                                                             │
│  ─────────────────────────────────────────────────────────  │
│                                                             │
│  תתי שרירים:                               [+ הוסף תת]     │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ בייספס (Biceps)                              [🗑️]  │   │
│  └─────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ טרייספס (Triceps)                            [🗑️]  │   │
│  └─────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ אמות (Forearms)                              [🗑️]  │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│                              [ביטול]    [💾 שמור]          │
└─────────────────────────────────────────────────────────────┘
```

#### פופאפ הוספת תת-שריר

```
┌─────────────────────────────────────────┐
│  הוספת תת-שריר                   [✖️]  │
├─────────────────────────────────────────┤
│                                         │
│  שם בעברית:   [________________]       │
│                                         │
│  שם באנגלית:  [________________]       │
│                                         │
├─────────────────────────────────────────┤
│                    [ביטול]  [הוסף]     │
└─────────────────────────────────────────┘
```

#### לשונית ציוד - UI

```
┌─────────────────────────────────────────────────────────────┐
│  ציוד                                      [+ הוסף ציוד]   │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ 🏋️ מוט (Barbell)                        סדר: 1       │ │
│  │                                    [✏️ ערוך] [🗑️ מחק] │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                             │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ 🏋️ משקולת יד (Dumbbell)                 סדר: 2       │ │
│  │                                    [✏️ ערוך] [🗑️ מחק] │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                             │
│  ...                                                        │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### פופאפ הוספה/עריכת ציוד

```
┌─────────────────────────────────────────┐
│  הוספת ציוד חדש                  [✖️]  │
├─────────────────────────────────────────┤
│                                         │
│  שם בעברית:   [________________]       │
│                                         │
│  שם באנגלית:  [________________]       │
│                                         │
│  סדר תצוגה:   [___]                    │
│                                         │
├─────────────────────────────────────────┤
│                 [ביטול]  [💾 שמור]     │
└─────────────────────────────────────────┘
```

#### פונקציות CRUD

```typescript
// ניהול שרירים
const handleAddMuscle = async (muscle: MuscleFormData) => {
  const id = generateSlug(muscle.name); // "upper_body" from "Upper Body"
  
  await muscleService.create({
    id,
    name: muscle.name,
    hebrewName: muscle.hebrewName,
    subMuscles: muscle.subMuscles,
    order: muscle.order,
    isActive: true
  });
  
  toast.success('השריר נוסף בהצלחה');
  refreshMuscles();
};

const handleEditMuscle = async (id: string, muscle: MuscleFormData) => {
  await muscleService.update(id, {
    name: muscle.name,
    hebrewName: muscle.hebrewName,
    subMuscles: muscle.subMuscles,
    order: muscle.order
  });
  
  toast.success('השריר עודכן בהצלחה');
  refreshMuscles();
};

const handleDeleteMuscle = async (id: string) => {
  // בדיקה: האם יש תרגילים שמשתמשים בשריר הזה?
  const exercisesUsingMuscle = await exerciseService.getByMuscle(id);
  
  if (exercisesUsingMuscle.length > 0) {
    toast.error(`לא ניתן למחוק - ${exercisesUsingMuscle.length} תרגילים משתמשים בשריר זה`);
    return;
  }
  
  const confirmed = await showConfirmDialog(
    'מחיקת שריר',
    `האם אתה בטוח שברצונך למחוק את "${muscle.hebrewName}"?`
  );
  
  if (!confirmed) return;
  
  await muscleService.delete(id); // מחיקה רכה: isActive = false
  toast.success('השריר נמחק');
  refreshMuscles();
};

// ניהול ציוד - אותו דבר
const handleAddEquipment = async (equipment: EquipmentFormData) => { ... };
const handleEditEquipment = async (id: string, equipment: EquipmentFormData) => { ... };
const handleDeleteEquipment = async (id: string) => { ... };
```

#### Validation

```typescript
const validateMuscle = (muscle: MuscleFormData): ValidationResult => {
  const errors: string[] = [];
  
  if (!muscle.hebrewName?.trim()) {
    errors.push('שם בעברית הוא שדה חובה');
  }
  
  if (!muscle.name?.trim()) {
    errors.push('שם באנגלית הוא שדה חובה');
  }
  
  if (!muscle.order || muscle.order < 1) {
    errors.push('סדר תצוגה חייב להיות מספר חיובי');
  }
  
  // בדיקת כפילויות
  const existingMuscle = muscles.find(m => 
    m.hebrewName === muscle.hebrewName && m.id !== muscle.id
  );
  if (existingMuscle) {
    errors.push('שריר עם שם זה כבר קיים');
  }
  
  return {
    isValid: errors.length === 0,
    errors
  };
};
```

#### קבצים ליצירה

```
src/
├── components/
│   └── admin/
│       ├── MuscleManagement.tsx       // לשונית שרירים
│       ├── MuscleForm.tsx             // פופאפ הוספה/עריכה
│       ├── MuscleCard.tsx             // כרטיס שריר בודד
│       ├── SubMuscleList.tsx          // רשימת תתי שרירים
│       ├── EquipmentManagement.tsx    // לשונית ציוד
│       ├── EquipmentForm.tsx          // פופאפ הוספה/עריכה
│       └── EquipmentCard.tsx          // כרטיס ציוד בודד
│
├── hooks/
│   ├── useMuscleManagement.ts         // CRUD + state לשרירים
│   └── useEquipmentManagement.ts      // CRUD + state לציוד
│
└── lib/firebase/
    ├── muscleService.ts               // כבר קיים
    └── equipmentService.ts            // כבר קיים
```

#### 12.12 בדיקות - ממשק ניהול

```
□ בדיקה 1: הוספת שריר חדש
  1. לחץ "+ הוסף שריר"
  2. מלא שם בעברית ובאנגלית
  3. הוסף תתי שרירים
  4. לחץ "שמור"
  
  ודא:
  ✓ השריר מופיע ברשימה
  ✓ השריר נשמר ב-Firebase
  ✓ תתי השרירים נשמרו

□ בדיקה 2: עריכת שריר קיים
  1. לחץ "ערוך" על שריר
  2. שנה את השם
  3. הוסף/הסר תת-שריר
  4. לחץ "שמור"
  
  ודא:
  ✓ השינויים נשמרו ב-Firebase
  ✓ התצוגה התעדכנה

□ בדיקה 3: מחיקת שריר
  1. לחץ "מחק" על שריר שאין לו תרגילים
  2. אשר בפופאפ
  
  ודא:
  ✓ השריר נעלם מהרשימה
  ✓ isActive = false ב-Firebase

□ בדיקה 4: מניעת מחיקה
  1. נסה למחוק שריר שיש לו תרגילים
  
  ודא:
  ✓ מופיעה הודעת שגיאה
  ✓ השריר לא נמחק

□ בדיקה 5: ניהול ציוד
  - חזור על בדיקות 1-4 עבור ציוד

□ בדיקה 6: שינויים משפיעים על המערכת
  1. הוסף שריר חדש
  2. עבור לספריית תרגילים
  
  ודא:
  ✓ השריר החדש מופיע בפילטרים
```

---

## 10. הנחיות עבודה לסוכן (Agent Guidelines)

### 10.1 עקרונות יסוד - קרא לפני שמתחיל!

> ⚠️ **אזהרה קריטית:**
> - אל תמחק קוד קיים בלי להבין מה הוא עושה
> - אל תשנה קבצים שלא קשורים למשימה
> - אל תשבור פונקציונליות קיימת
> - בצע את העבודה בשלבים קטנים עם בדיקות ביניים

### 10.2 שלבי העבודה (חובה לעקוב!)

```
שלב 1: הכרת המערכת (לפני כל שינוי)
─────────────────────────────────────
□ קרא את כל הקבצים הרלוונטיים
□ הבן את המבנה הקיים
□ זהה את קובץ העיצוב המרכזי (theme/tokens)
□ זהה את הקומפוננטות הקיימות
□ רשום לעצמך: מה קיים ומה חסר

שלב 2: תכנון (לפני כתיבת קוד)
─────────────────────────────────────
□ רשום רשימת קבצים שצריך ליצור
□ רשום רשימת קבצים שצריך לעדכן
□ ודא שאתה לא יוצר duplicate של משהו קיים
□ תכנן את סדר הפעולות

שלב 3: עיצוב (Design Tokens)
─────────────────────────────────────
□ בדוק אם הצבעים/גדלים שאתה צריך קיימים ב-tokens
□ אם חסר משהו - הוסף לקובץ המרכזי בלבד
□ אל תכתוב צבעים/גדלים hardcoded בקומפוננטות!

שלב 4: מימוש בשלבים קטנים
─────────────────────────────────────
□ צור קובץ אחד → בדוק שהאפליקציה עולה
□ עדכן קובץ אחד → בדוק שלא נשבר כלום
□ חזור על התהליך לכל קובץ

שלב 5: בדיקות (חובה!)
─────────────────────────────────────
□ בצע את כל הבדיקות מסעיף 9
□ מלא את דוח הבדיקה
□ תקן בעיות שנמצאו
```

### 10.3 חוקי העיצוב - קריטי!

#### ❌ אסור - Hardcoded Styles

```typescript
// ❌ אסור! לעולם אל תכתוב כך:
<div style={{ backgroundColor: '#EF4444', borderRadius: '8px' }}>

// ❌ אסור! גם ב-className:
<div className="bg-red-500 rounded-lg">
```

#### ✅ חובה - שימוש ב-Design Tokens

```typescript
// ✅ נכון! הפניה לקובץ המרכזי:
import { tokens } from '@/theme/tokens';
import { colors } from '@/theme/colors';

<div style={{ 
  backgroundColor: tokens.colors.status.planned,
  borderRadius: tokens.borderRadius.md 
}}>

// ✅ נכון! שימוש בקלאסים מוגדרים:
<div className={styles.plannedWorkoutCard}>
```

#### מיקום קובץ העיצוב המרכזי

```
src/
└── theme/
    ├── tokens.ts      ← כל הצבעים, גדלים, spacing
    ├── colors.ts      ← פלטת צבעים
    └── styles.ts      ← סגנונות משותפים
```

#### הוספת צבע חדש - הדרך הנכונה

```typescript
// 1. קודם - הוסף לקובץ tokens.ts:
export const tokens = {
  colors: {
    status: {
      completed: '#3B82F6',   // כחול
      inProgress: '#F59E0B',  // צהוב
      planned: '#EF4444',     // אדום ← הוסף כאן אם חסר
    }
  }
};

// 2. אחר כך - השתמש בקומפוננטה:
import { tokens } from '@/theme/tokens';

const statusColor = tokens.colors.status.planned;
```

### 10.4 חוקי הלוגיקה והשמירה

#### מבנה הקבצים - הפרדת אחריות

```
src/
├── hooks/
│   └── useCreateWorkout.ts    ← לוגיקה בלבד, ללא UI
│
├── lib/firebase/
│   └── workoutService.ts      ← פעולות Firebase בלבד
│
├── components/
│   └── DatePickerField.tsx    ← UI בלבד, ללא לוגיקת שמירה
│
└── pages/
    └── ExerciseSelectionPage.tsx  ← מחבר הכל יחד
```

#### כתיבת Hook - תבנית

```typescript
// hooks/useCreateWorkout.ts

export const useCreateWorkout = () => {
  const navigate = useNavigate();
  const { user } = useAuth();
  
  const createWorkout = async (
    exercises: Exercise[],
    selectedDate: Date | null
  ) => {
    // 1. Validation
    if (!exercises.length) {
      throw new Error('נדרש לפחות תרגיל אחד');
    }
    
    // 2. Determine flow
    const flow = determineWorkoutFlow(selectedDate);
    
    // 3. Build document
    const workout = buildWorkoutDocument(exercises, flow, user.uid);
    
    // 4. Save to Firebase
    await workoutService.create(workout);
    
    // 5. Navigate
    if (flow.navigateTo === 'active-workout') {
      navigate(`/workout/${workout.id}`);
    } else {
      toast.success(`האימון נשמר לתאריך ${formatDate(flow.scheduledDate)}`);
      navigate('/workout-planning');
    }
  };
  
  return { createWorkout };
};
```

#### כתיבת Service - תבנית

```typescript
// lib/firebase/workoutService.ts

export const workoutService = {
  // יצירה
  create: async (workout: WorkoutDocument): Promise<string> => {
    const docRef = await addDoc(collection(db, 'workouts'), workout);
    return docRef.id;
  },
  
  // קריאה
  getById: async (id: string): Promise<WorkoutDocument | null> => {
    const doc = await getDoc(doc(db, 'workouts', id));
    return doc.exists() ? doc.data() as WorkoutDocument : null;
  },
  
  // עדכון
  update: async (id: string, data: Partial<WorkoutDocument>): Promise<void> => {
    await updateDoc(doc(db, 'workouts', id), data);
  },
  
  // מחיקה
  delete: async (id: string): Promise<void> => {
    await deleteDoc(doc(db, 'workouts', id));
  }
};
```

### 10.5 רשימת בדיקות ביניים (בין כל שלב)

```
□ האפליקציה עולה ללא שגיאות בקונסול
□ אין שגיאות TypeScript
□ הדפים הקיימים עדיין עובדים
□ הניווט תקין
□ אין רגרסיה בפונקציונליות קיימת
```

### 10.6 מה לעשות אם משהו נשבר

```
1. עצור מיד - אל תמשיך לשנות
2. זהה מה השינוי האחרון שעשית
3. בטל את השינוי האחרון (git revert או ידנית)
4. ודא שהאפליקציה עובדת שוב
5. נתח למה נשבר
6. תכנן גישה אחרת
7. המשך בזהירות
```

### 10.7 צ'קליסט סיום משימה

```
לפני שמדווח על סיום:

□ כל הבדיקות מסעיף 9 עברו
□ אין שגיאות בקונסול
□ אין שגיאות TypeScript
□ כל העיצוב משתמש ב-tokens (אין hardcoded)
□ הקוד מאורגן לפי מבנה הקבצים
□ דוח הבדיקה מלא ומצורף
□ לא נשברה פונקציונליות קיימת
```

---

## היסטוריית שינויים

| גרסה | תאריך | שינויים |
|------|-------|---------|
| 1.0 | 03/01/2026 | מסמך ראשוני - זרימת יצירת אימון בסיסית |
| 1.1 | 03/01/2026 | הוספת סעיף 9 - הנחיות לסוכן ובדיקות עצמיות |
| 1.2 | 03/01/2026 | הוספת סעיף 10 - הנחיות עבודה מפורטות לסוכן |
| 1.3 | 03/01/2026 | הוספת סעיף 11 - המשך לאימון ממסך היסטוריה |
| 1.4 | 03/01/2026 | הוספת סעיף 11.9 - הבהרת ניווט למסך אימון פעיל |
| 1.5 | 03/01/2026 | הוספת סעיף 12 - מבנה נתונים ב-Firestore (muscles, equipment) |
| 1.6 | 03/01/2026 | תיקון מבנה שרירים - הוספת תתי שרירים, זרועות כשריר ראשי |
| 1.7 | 03/01/2026 | הוספת סעיף 12.11 - ממשק ניהול שרירים וציוד (Admin UI) |

---

*מסמך זה יתעדכן בהדרגה עם הוספת יכולות נוספות*
